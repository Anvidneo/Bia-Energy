// Package ai is the explanation layer, decoupled from classification via
// an Explainer interface. RuleExplainer (template-based, deterministic) is
// the Friday deliverable; LLMExplainer is an optional Sunday stretch goal
// behind the USE_LLM_EXPLAINER flag.
package ai
