// Package firebase is an optional, isolated client that publishes
// critical-severity anomaly alerts to Firestore. Any error here must never
// interrupt the main request flow. Stretch goal, only if time allows.
package firebase
