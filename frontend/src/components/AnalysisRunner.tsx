// Visual pipeline state while analysis runs (Lecturas -> Baseline -> ... ->
// Recomendación), polling GET /ai/analysis/:id every ~500ms while
// "processing". Placeholder for today's skeleton — real content Saturday.
export function AnalysisRunner() {
  return (
    <section>
      <h2>Analysis runner</h2>
      <p>TODO: estado visual del pipeline de análisis.</p>
    </section>
  )
}
