// Meter list + summary cards (GET /dashboard/summary). Placeholder for
// today's skeleton — real content on Saturday.
export function Dashboard() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>TODO: lista de medidores + tarjetas de resumen.</p>
    </section>
  )
}
